server:
  port: 8080

spring:
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/sample
    username: root
    password: 123456789

api:
  security:
    token:
      secret: ${JWT_SECRET:my-secret-key}
